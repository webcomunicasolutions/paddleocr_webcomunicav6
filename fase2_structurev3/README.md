# Fase 2 — PP-StructureV3 (tablas/layout)

Aquí irá el `Dockerfile` + config para desplegar **PP-StructureV3** en GPU (parsing de documento → tablas/layout → JSON/Markdown).
Puerto sugerido: 9102. Criterio éxito: mejor recuperación de tablas/totales que baseline.

_(pendiente de rellenar al arrancar la Fase 2)_
