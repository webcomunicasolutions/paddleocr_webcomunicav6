# Fase 1 — PP-OCRv5 (re-test en GPU)

Aquí irá el `Dockerfile` + config para desplegar PaddleOCR con **PP-OCRv5** en GPU.
Base: partir del stack de producción v5.6 cambiando `OCR_VERSION` y los model names a PP-OCRv5, sobre `paddlepaddle-gpu`.
Puerto sugerido: 9101. Criterio éxito: ≥ baseline (recordar: en CPU dio −3.4%).

_(pendiente de rellenar al arrancar la Fase 1)_
